/**
 * The component Library that contains all the word entries the user ever stored.
 */
function Library() {
  return <div data-testid="library-container">Library Component</div>;
}

export default Library;
